#import "./template.typ": *
#import "@preview/akatable:0.1.0": academic-table
#show: template
#show: tmp_page

#print_title("双目实时三维重建：完整信号处理流程")

= 人类视觉先验调研
== 为什么需要先验：问题的不适定性
=== 逆问题与 Helmholtz 无意识推理

从二维投影恢复三维场景在数学上高度不适定：无穷多个三维场景可以产生完全相同的视网膜图像。传统三维重建用多目几何或深度传感器增加约束，代价是硬件复杂度与成本；而人类仅凭双眼（甚至单眼静态图像）就能稳定地感知三维世界，说明大脑在感觉输入之外注入了强约束——即视觉先验。

Helmholtz（1867）将此概括为"无意识推理"（unconscious inference）：感知不是对输入的直接读取，而是大脑利用过去经验对模糊感觉输入做出的最佳假设。透视关系、光照与阴影、纹理梯度、语义理解都被用来补全缺失的深度信息。这一思想是现代贝叶斯感知理论的直接源头。

=== Marr 的计算视觉理论
Marr（1982）#cite(<marr1982vision>) 把视觉系统分析分为三个层次：

- *计算理论层*：要解决什么问题，为什么需要约束；
- *算法层*：用什么表征与算法施加约束；
- *实现层*：神经机制如何承载这些算法。

在计算理论层面，任何求解不适定问题的视觉系统都必须显式或隐式地施加约束，这些约束就是先验。Marr 提出的 2.5D 草图（surface 法线、深度梯度、边界的中间表征）本身就是一种结构化先验：它先于完整三维重建存在，为后续推断提供基底。

=== 贝叶斯形式化 <贝叶斯形式化>
Bayesian 视觉模型将先验形式化为概率分布，感知即最大后验推断：

$ Ŝ = "argmax"_S P(S|I) = "argmax"_S P(I|S)·P(S) $

似然 `P(I|S)` 对应成像过程，先验 `P(S)` 对应自然世界的统计规律。
代表性工作：Knill & Richards (1996) 的深度线索整合、Weiss et al. (2002)
的运动感知（低速先验解释运动孔径问题）、Yuille & Bülthoff (1996)
的光照与形状推断。精确贝叶斯推断通常不可计算，因此大脑被认为使用
变分推断、粒子滤波、抽样等近似算法。

=== 格式塔学派：先验的经验源头 <格式塔学派>
在计算理论之前，Gestalt 学派（Wertheimer 1923；Koffka 1935）已从
心理物理实验中归纳出一组知觉组织律：邻近性、相似性、闭合性、
良好连续性（good continuation）、共同命运（common fate）、
图形-背景分离。这些定律本质上就是先验假设的现象学描述，后来大多
获得了几何概率（一般视角假设）或自然图像统计（良好连续性与
边缘共圆性统计）层面的形式化解释。

== 先验假设分类体系
按作用域分为六类。每类给出机制描述、关键证据与对计算机视觉的启示。
=== 半透明与分层先验（Transparency & Reflection） <半透明与分层先验>

- *分层表示*：大脑构建的不是单一深度图，而是类 2.5D 草图的多层
  结构——稳定的底层（Base Layer）与高亮、低对比度或特定位移的
  反光/透明层。感知过程相当于从 $I = L_1 + L_2$ 中迭代解耦两个层，直到每层在语义上自洽（L₁ 像一棵树，L₂ 像一扇窗）。
- *X 结与 Metelli 约束*：半透明覆盖在边缘交叉处形成 X 形连接。
  只有当交叉点亮度变化符合物理"混合定律"（叠加后亮度介于两层
  之间）时，大脑才产生透明感（Metelli 1974）。
- *聚焦与模糊线索*：景深先验——清晰背景上的轻微模糊倒影（或
  反之）会被迅速归入不同深度层，频率差异是分离信号的重要判据。

对 CV 的启示：软分配（soft assignment）比硬分割更接近生物策略——
保留像素属于多层的后验，而非过早 argmax。

=== 几何与结构先验 <几何与结构先验>

#academic-table(
  [],
  columns: 3,
  format: "ieee",
  header: ([先验], [内容], [形式化证据]),
  (
    [对称性],
    [生物与人造物体多对称，\ 大脑自动补全不可见半侧],
    [镜像/旋转对称检测],
    [紧凑性与平滑性],
    [表面大多平滑，边缘与纹理变化稀疏],
    [正则化项 $E = E_("data") + lambda E_("smooth")$],
    [平直与正交],
    [室内/城市场景默认平面与直角],
    [曼哈顿世界假设],
    [线性透视],
    [平行线远方汇聚],
    [Gibson (1950) 纹理梯度],
    [部件组合],
    [复杂物体分解为几何原形组合],
    [Biederman (1987) Geon 理论],
    [物体边界],
    [边界对应强梯度],
    [边缘检测的本体论地位],
    [最小刚性],
    [多解时选刚性程度最高的解释],
    [Ullman (1979) SfM：3 视角 4 点定结构],
    [一般视角],
    [观察者不处于巧合视角],
    [Freeman (1994) 最小规则；巧合对齐导致知觉不稳],
    [凸形状偏好],
    [模糊形状解释为凸而非凹],
    [空心面具错觉（Ramachandran 1988）],
  ),
)

=== 光学先验 <光学先验>
- *光源上方先验（light-from-above）*：人类最核心的偏见。默认光从
  上方照射，"上亮下暗"被判为凸起；图像旋转 180° 会使凹凸感知反转。
  空心面具错觉（Ramachandran 1988）是最强证据——凹面具因与上方
  光照一致而被强制感知为凸脸。
- *单一光源先验*：尽管真实场景多光源，大脑倾向假设单一主光源，
  大幅简化 shape-from-shading#cite(<horn1970shape>)。
- *白平衡/光源颜色恒常性*：假设光源接近中性色，整体色相偏移会
  被自动校正（Land & McCann 1971 Retinex）。
- *反射率恒常性（Albedo constancy）*：亮度变化优先解释为形状转折
  的阴影而非表面换漆。Adelson 棋盘阴影错觉是该先验的极端演示。
- *色相线索*：阴影处色相对光源补色的偏移也携带深度信息。

对 CV 的启示：光照归一化必须在特征层完成而非仅在图像层。

=== 物理先验 <物理先验>
- *物体连续性*：材质、纹理属性空间连续。
- *重力感*：物体受重力约束，大脑自动寻找支撑关系；底部无支撑的物体触发"挂在哪里/正在掉落"的推断。
- *视平线与高度*：地平面上的物体越接近视平线越远。
- *大气透视*：远处物体对比度低、偏蓝、边缘模糊。
- *立体视差与运动视差*：双眼视差#cite(<cumming1997neural>)与头动产生的相对运动是最直接的度量深度线索。
- *遮挡逻辑*：轮廓 T 结——A 的轮廓切断 B，则 A 在前。这是最稳固的深度顺序判据，也是 Pb 边界图的语义基础。

=== 运动与时间先验 <运动与时间先验>
- 运动连续性、速度一致性、加速度限制：物体运动轨迹平滑，突变需要外力解释。
- 时间一致性：形状与位置在相邻帧不剧变。
- 不变性假设：颜色、纹理等属性跨视角保持稳定——这是"同一物体"判断的根基，也是跨帧聚类一致性的依据。

=== 语义与经验先验 <语义与经验先验>
- *物体类别模板*：识别出"椅子"后直接调用三维模板，而非从零计算长宽高。现代对应物：类别级形状先验、检索式重建。
- *熟悉大小*：指甲盖大的"小人"不会被判为侏儒，而是由"人高约1.7 m"反推其距离。单目深度网络学到的相当部分就是这种语义-尺寸-距离的联合统计。

= 神经机制

== 层级处理与两条通路

先验在视觉层级#cite(<felleman1991distributed>)不同水平上实现：

- *早期视觉（V1, V2）*：局部方向与空间频率滤波#cite(<hubel1962receptive>)；Gabor 感受野模型 #cite(<jones1987evaluation>)；横向连接实现局部连续性约束，关联场（association field, #cite(<field1993contour>)）促进共圆边缘整合成长轮廓；非经典感受野抑制纹理背景、凸显轮廓#cite(<grigorescu2003contour>)。
- *中层视觉（V3, V4, MT）*：形状、运动、深度整合；更大空间范围的约束传播；表面表征形成。
- *高层视觉（IT, PPA, FFA）*：物体识别、语义先验整合、记忆与预期调制。背侧通路（where/how）处理空间与运动，腹侧通路（what）处理识别#cite(<ungerleider1982two>)；#cite(<goodale1992separate>) 进一步提出"感知"与"行动"的功能二分。Serre et al.#cite(<serre2007feedforward>)证明纯前馈层级架构足以解释快速视觉分类，为层级处理的计算充分性提供了关键证据。

== 自上而下反馈与预测编码 <自上而下反馈与预测编码>

先验通过反馈连接实现。预测编码框架#cite(<rao1999predictive>)：高层生成对低层信号的预测，预测误差向上传播，先验体现为生成模型的参数。证据：反馈连接病变损害形状知觉；注意与预期调制早期视觉响应；模糊刺激下高层区域 fMRI 活动增强。

== 稀疏编码与自然图像统计

Barlow 高效编码假说#cite(<barlow1961possible>)：感觉系统应以最小冗余表征输入。Olshausen & Field#cite(<olshausen1996emergence>)证明在自然图像统计下学习稀疏码，基函数自发收敛为类 V1 简单细胞的局部化、带方向、带空间频率的 Gabor 型滤波器——即 V1 的"硬件架构"本身就是自然图像先验的物化。自然图像的 1/f 功率谱、尺度不变性、边缘共圆性统计构成可被内化的先验分布（Simoncelli & Olshausen 2001 综述）。

== 可塑性与个体差异

- 关键期（双目视觉、面孔加工）与经验依赖的精细调整；跨文化差异（如 Müller-Lyer 错觉强度）说明部分先验由环境统计塑造。
- 成人感知学习与适应：短期暴露改变先验权重，可用贝叶斯观察者模型的先验重估解释（Chalk et al. 2010）。
- 先验强度存在个体差异，与认知风格相关（自闭谱系倾向弱先验）。

= 功能模块划分

上述六类视觉先验与神经机制的系统梳理揭示了一个关键事实：深度感知并不依赖单一通路，而是多条通路并行计算、最终贝叶斯融合的结果。最直接的证据来自日常经验——闭上一只眼睛后我们仍然能够感知三维世界（尽管精度下降），说明双目视差是增强手段而非必要前提。本节据此将重建管线重构为双路径并行架构：一条单目通路始终可用，一条双目通路在有条件时增强精度；同时，注意力与预测编码不再占据独立的流水线位置，而是作为跨层调制机制渗透于每一模块。八个功能模块及其信息流如 @fig-module-flow 所示。

#figure(
  image("artifacts/module-flow-v2.png", width: 100%),
  caption: [双路径并行架构：单目通路（绿色）始终可用，双目通路（橙色）有条件增强精度；灰色虚线为反馈连接；注意与预测编码为跨层机制],
) <fig-module-flow>

== 单眼特征层

单眼特征层对应早期视觉皮层（V1/V2）的局部滤波功能，对左右眼图像分别独立处理，提取多尺度、多方向的低级特征表征。它是整条管线的计算基底，同时服务于两条深度通路。

- *Gabor 小波变换作为默认前端*：V1 简单细胞的感受野可精确建模为不同方向、空间频率、相位的 Gabor 滤波器#cite(<jones1987evaluation>)。稀疏编码理论证明，在自然图像统计下学习最优基函数会自发收敛为 Gabor 型滤波器#cite(<olshausen1996emergence>)——Gabor 分解本身即是自然图像先验的内化：$1/f$ 功率谱、尺度不变性与边缘共圆性统计被编码于滤波器的参数选择中。工程实现上，Freeman & Adelson#cite(<freeman1991design>)的可操纵滤波器理论为高效多方向 Gabor 滤波提供了计算基础。Kovesi#cite(<kovesi1999image>)的相位一致性理论进一步表明：边缘不依赖于局部幅度响应，而体现为不同尺度傅里叶分量的相位对齐——Gabor 滤波器组输出的相位信息为此类特征检测提供了天然适配。
- *色度与亮度通道分离*：视网膜与 LGN 将信号分解为亮度与色度拮抗通路#cite(<livingstone1984anatomy>)。采用色相-饱和度复数表示 $Z = S dot e^(i H)$，使色度独立于亮度，为颜色恒常性、反射率恒常性（@光学先验）与半透明解耦（@半透明与分层先验）提供特征基底。该复数编码在数学上承袭 Felsberg & Sommer#cite(<felsberg2001monogenic>)的单演信号（monogenic signal）思路——将多维视觉信号分解为局部幅度、相位与方向的统一表征。
- *@光学先验 的特征层级实现*：Retinex 中心-环绕对比归一化（Land & McCann 1971）、白平衡校正及光源上方先验的方向性增益调制，均在特征层而非像素层实施。非经典感受野的环绕抑制#cite(<grigorescu2003contour>)为此类局部归一化提供了直接的神经对应，与 divisive normalization 的规范计算理论#cite(<heeger1992normalization>)#cite(<carandini2012normalization>)一致。

== 知觉组织层

知觉组织层将单眼特征层输出的局部特征按 Gestalt 组织律聚合成具有感知意义的中间基元——轮廓片段、同质纹理区域、共运动团块。这些基元是后续两条深度通路共享的通用表征：轮廓和 T 结直接提供遮挡的深度顺序；纹理梯度区域携带线性透视信息；运动团块则驱动运动视差计算。

- *Gestalt 组织律的计算化*：空间邻近性、特征相似性与共同命运（common fate）——位置接近、特征向量相似、运动方向一致的像素/特征点应归入同一组（@格式塔学派）。这是知觉组织的最基本操作，也是场景分割与深度线索提取的共同前处理。
- *关联场与轮廓整合*：V1 横向连接#cite(<bosking1997orientation>)形成的关联场#cite(<field1993contour>)偏好共圆、共线边缘的连接，实现了良好连续性先验的计算化。在工程上可近似为张量投票或基于边缘共圆统计的图割。
- *非经典感受野抑制与边界提取*：纹理背景中孤立边缘被抑制、轮廓被凸显。该机制直接对应 Pb 边界图#cite(<arbelaez2011contour>)的局部计算——仅保留不同质区域间的强边界。
- *遮挡 T 结检测*：轮廓 T 结是 @物理先验 中遮挡逻辑的最小可计算单元——检测"轮廓 A 被轮廓 B 截断"直接决定局部深度顺序（A 在 B 前）。V2 中的 border ownership 编码#cite(<zhou2000coding>)是此操作的神经对应。这一信息同时流向单目深度线索（作为遮挡排序的硬约束）与双眼视差估计（限定视差搜索的半遮挡区域）。
- *纹理区域分割*：同质纹理区域通过 Gabor 响应的空间统计相似性（局部能量谱的余弦距离）聚合——纹理基元的位置、方向与尺度一致性定义了区域的几何边界，同时也是单目深度线索中纹理梯度计算的输入单元。
- *共运动分组*：跨帧光流方向与速度一致的像素聚合成共运动团块。共同命运是 Gestalt 诸定律中唯一包含时间维度的原则——同一团块的像素几乎必然属于同一物理表面或物体，这是时空累积层中跨帧物体跟踪的起点。

== 场景分割层

场景分割层将场景分解为语义上有意义的物体实例与背景区域，同时处理半透明、反光等非朗伯表面的分层表示。与前一版架构的关键区别在于：本层不再位于固定的串联位置，而是与深度融合层形成双向交互——分割提供语义先验给深度估计，深度反馈则修正分割边界。

- *@半透明与分层先验 的多层表示*：大脑构建类 2.5D 草图的多层结构——稳定基底层与高亮/低对比度的反光层并存。感知等价于从 $I = L_1 + L_2$ 中迭代解耦两个层，直到每层语义自洽。X 结的 Metelli 亮度约束是解耦的物理判据。*关键工程启示*：保持软分配而非过早硬分割。
- *Gestalt 图形-背景分离的语义化*：图形-背景的感知不对称性——图形有确定形状、背景无定形延伸——是人类视觉的基本组织原则。分割结果标注物体轮廓与纹理/阴影边缘的类别差异。
- *@语义与经验先验 的模板注入*：物体类别识别（IT 皮层）后调用三维模板，不必从零计算几何参数。Biederman（1987）的 Geon 理论表明 36 种几何原形可覆盖大多数日常物体——部件组合先验（@几何与结构先验）在此转化为可计算的形状基元匹配。
- *与深度融合的双向交互*：分割结果将物体边界与熟悉大小前馈至深度融合层，约束深度不连续的位置并锚定绝对尺度；深度融合层将当前深度估计反馈回分割层，位于同一深度的像素倾向于归入同一物体，深度断裂处提示物体边界。这两层的双向信息流构成了 @自上而下反馈与预测编码 在中层视觉的核心实例。

== 单目深度线索

单目深度线索是本架构的核心创新——一条完全独立于双目的深度通路，仅依赖单眼可用信息推断深度。正是这条通路解释了为什么闭上一只眼睛后，人类仍然能够稳定感知三维世界（精度虽有下降，结构并不坍塌）。多条线索并行提取，每条对应一类视觉先验：

- *shape-from-shading（@光学先验）*：光源上方先验驱动——"上亮下暗"解释为凸起。单一光源假设大幅简化计算。
- *纹理梯度（@几何与结构先验）*：规则纹理在透视投影下产生可预测的尺度与密度变化（Gibson 1950），从中可反推表面倾斜与距离。
- *遮挡顺序（@物理先验）*：知觉组织层输出的 T 结提供最稳固的局部深度排序——A 截断 B 则 A 在前。在数学上，遮挡提供的是序数深度而非度量深度，但足够构建深度层级的骨架。
- *熟悉大小（@语义与经验先验）*："人高约 1.7 m"反向推算距离——语义识别结果注入此通路，提供绝对尺度参考。
- *大气透视（@物理先验）*：远处物体对比度低、偏蓝、边缘模糊——低层特征中的频率与色相偏移携带绝对深度信号。
- *视平线高度（@物理先验）*：地平面物体越近视平线越远——这一几何关系将图像坐标映射到世界距离。

== 双眼视差估计

双眼视差估计是并行架构中的双目深度通路。它将旧架构中的"双眼特征对应"与"双眼汇聚层"合并为一个端到端模块。该通路仅在双目输入可用时激活，其输出是高质量的视差图；在单目场景下该通路自然旁路，不影响系统运行。

- *稀疏到稠密的匹配策略*：先在高置信度的角点、边缘端点与 T 结上建立稀疏对应（利用知觉组织层输出的稳定基元），再以此为锚点扩散至全图稠密匹配。V1 中双眼细胞的视差调谐特性#cite(<ohzawa1990stereoscopic>)；#cite(<cumming1997neural>)是该策略的神经对应。
- *极线约束与唯一性先验*：对极几何将二维搜索降为一维。唯一性先验（每个左眼特征至多匹配一个右眼特征）施加稀疏正则化。@几何与结构先验 中的紧凑性与平滑性先验（$E = E_"data" + lambda E_"smooth"$）使无纹理区域可解。
- *多尺度粗到精策略*：与 Gabor 小波组的多尺度特性自然衔接——粗尺度确定大范围视差，细尺度逐级精化。
- *通路独立性*：本通路接受知觉组织层的通用基元作为输入，输出为视差图。其停用（单目输入）不影响单目深度通路的正常运行——下游深度融合层会自然地将双目线索的可靠性权重降为零。
- *匹配代价函数*：基于 Gabor 相位差异与色度向量的联合相似度度量，对光照变化具有天然鲁棒性（相位对亮度增益不敏感）。代价聚合采用引导滤波或半全局匹配（SGM），在保边的同时传播高置信度匹配到无纹理区域。
- *亚像素精化*：在整像素视差基础上，通过局部代价函数的二次拟合或相位线性插值，将视差精度提升至亚像素级别。这对远距离物体的深度估计尤为关键——远距离视差值小，整像素量化误差在深度域被非线性放大。

== 深度融合层

深度融合层是两条并行深度通路的汇合点。它以贝叶斯方式将单目深度线索和双眼视差估计的结果融合为统一的深度图——Marr 2.5D 草图的计算实现。

- *多线索贝叶斯整合*：Knill & Richards（1996）指出深度线索应按可靠性加权整合，形式为最大后验推断 $Ŝ = "argmax"_S P(I|S)·P(S)$。关键之处在于：每种线索的可靠性是动态的——近距离双目视差的权重较高，远距离（视差趋近于零）则单目线索主导。在单目输入下，双目线索的可靠性自然为零，退化为纯单目推断。这正对应 @贝叶斯形式化 描述的变分推断策略。
- *场景分割的双向交互*：场景分割层提供的物体边界限定深度不连续的位置（物体内部深度应平滑、物体边界处允许断裂）；当前深度估计反馈回场景分割层，帮助区分纹理边缘与物体轮廓。
- *运动视差注入（@运动与时间先验）*：时空累积层估计的自运动与场景流在此作为第三条深度线索汇入融合——运动视差在快速头动时甚至可以替代立体视差（@物理先验）。
- *遮挡逻辑的高权重编码*：T 结提供的深度顺序先验（@物理先验）是唯一不需要度量校正的线索——"A 在 B 前"这个序数判断在任何距离都成立。应在贝叶斯融合中以极高先验权重编码。
- *逐像素置信度估计*：融合层不仅输出深度值，还输出每个像素的后验方差 $op("Var")[S mid I]$。高方差区域（如无纹理墙面、镜面反射）标记为不可靠，降低其在时空累积与三维重建中的传播权重；低方差区域（如强纹理边缘、双目高置信度匹配点）作为高精度锚点。
- *线索冲突仲裁*：当两条通路（如 shape-from-shading 与立体视差）在同一像素给出矛盾深度时，按各自当前可靠性加权折扣——若某一通路持续被其他通路"投票否决"，其全局可靠性参数下调。这等价于为每条通路维护一个动态的线索可靠性超参数 $omega_c$，以在线 EM 方式更新。*关键设计*：冲突仲裁与 Bayesian 整合共享同一数学框架，不引入独立模块。

== 时空累积层

时空累积层跨帧传播信息、检测时域一致性，并将运动线索转化为深度信息反馈给融合层。它是动态场景重建的信息倍增器，也是运动视差这一重要深度线索的来源。

- *@运动与时间先验 的计算化*：运动连续性、速度一致性（低速先验，Weiss et al. 2002）与加速度限制在计算上等价于 Kalman 滤波或粒子滤波的状态转移模型——上一帧的深度融合结果作为当前帧的先验，仅预测误差需要重新计算（@自上而下反馈与预测编码 的时域展开）。
- *运动视差与自运动估计*：观察者自身运动产生的视差流是强大的深度线索——快速移动时甚至可替代立体视差。本层应同时估计自运动（egomotion）与场景流，将运动场分解为观察者分量与场景分量。场景流输入深度融合层作为第三条深度通路。
- *不变性假设与跨帧一致性*：颜色、纹理等属性跨视角保持稳定——这是知觉组织层中共同命运原则的时域延伸，也是跨帧物体跟踪的基石。

== 三维重建层

三维重建层在所有前置模块的输出基础上构建完整的三维场景模型，是 Marr 三维模型层的工程实现与整个管线的终点。

- *@几何与结构先验 的结构化约束*：曼哈顿世界假设（默认正交平面）、对称性补全（镜像/旋转对称自动补全被遮挡半侧）、紧凑性与平滑性正则化——这些先验在此层被显式编码为优化问题的约束项或正则项，而非仅作为隐式网络偏置。
- *@语义与经验先验 的类别级形状*：识别出"椅子"后直接调用参数化三维模型（类别级形状先验），大幅减少从零重建的计算代价。检索式重建是现代工程对应。
- *部件组合与层级结构*：复杂物体分解为几何原形组合（Geon 理论），以部件-整体层级图表示——既压缩表示复杂度，又利于被遮挡部件的推理。
- *多层三维输出*：输出为基底层 + 反光/透明层的多层三维表示。光照与反射率分离（intrinsic image decomposition）是使重建结果可重光照、可编辑的关键——这呼应了 @半透明与分层先验 的软分配原则。
- *几何代数表示*：整套管线的最终输出采用三维共形几何代数（CGA）表示，而非传统网格或点云。这一选择的核心动机是机器人后续任务的衔接——在 CGA 中，点、线、平面、球体均为一级代数对象（blade），刚体变换统一为 motor（rotor 与 translator 的几何积），无需分别维护旋转矩阵与平移向量。具体而言：(1) 抓取规划中，手指接触面为平面 blade，接近方向为线 blade，抓取稳定性判据直接表达为 blade 间的交并运算；(2) 导航与避障中，障碍物以球体 blade 包络，路径规划转化为 blade 间的 signed distance 计算；(3) 正向/逆向运动学中，关节链的位姿传播退化为 motor 的连续几何积，避免了欧拉角或四元数的奇异性与切换逻辑。从计算角度看，该层不重新推导 CGA 中的所有几何关系——而是将深度融合层产出的 2.5D 深度图升维为 CGA 中的点云，再通过几何先验（曼哈顿约束 $→$ plane blade 正交性、对称性 $→$ motor 反射关系）结构化地装配为参数曲面与物体的 blade 层级表示。*这一设计使重建结果不再是"观赏用"的视觉输出，而是可直接被机器人规划器消费的几何数据库。*
- *预测编码闭环*：重建结果反馈给深度融合层、场景分割层与单眼特征层，作为下一帧的预测先验——形成贯穿全部八层的自上而下预测通路，实现"感知即验证"的持续循环（@自上而下反馈与预测编码）。

== 跨层机制：注意力与预测编码

在原架构中"注意力层"是一个独立的串行阶段，但神经证据表明注意调制在视觉层级每一层都存在——从 V1 的对比度增益到 IT 的类别选择。因此在本架构中，注意力不再占据流水线位置，而是与预测编码一起作为渗透全部模块的跨层调制机制：

- *自底向上显著性*：高对比度、独特色相、运动突变等低层特征在任何层均可触发注意加权——在特征层增强显著区域的 Gabor 响应幅度，在知觉组织层优先聚合高显著性的基元，在深度融合层提高意外深度变化的可靠性权重。这在计算上等价于 Barlow 高效编码假说：系统应优先编码统计上"意外"的信号。Itti & Koch#cite(<itti1998model>)的显著性模型是其最经典的计算实现；Itti & Baldi#cite(<itti2005bayesian>)进一步以 Bayesian surprise 形式化此原则。
- *自顶向下的预测误差调制*：预测编码框架将每一层的注意权重与该层的预测误差绑定——与当前假设（来自更高层的预测）不符的区域获得更高的注意权重与处理精度。反馈连接（IT → V4 → V2，背侧通路 → MT/V1）是这一调制的解剖学基础。
- *Gestalt 图形-背景先验的跨层体现*：注意力天然偏向图形而非背景——@格式塔学派 的图形-背景分离定律在特征层（非经典感受野抑制纹理、凸显轮廓）、知觉组织层（优先聚合封闭轮廓）和场景分割层（物体实例与背景的分配）均有对应机制。

== CGA 接口规范

三维重建层以共形几何代数（CGA）blade 输出的决策，定义了前序模块的接口语义。各模块不改内部算法，仅统一输出类型：

- *深度融合层*：输出深度标量 + 表面法向 blade（平面 blade 在像平面上的投影） + 不连续阶跃方向。
- *时空累积层*：输出深度 + motor（6 参数刚体运动，替代 $4 times 4$ 变换矩阵）。
- *双眼视差估计*：输出视差 + 左右视线 blade，三角化直接产出 CGA 点。
- *单目深度线索*：shape-from-shading 输出法向 blade，纹理梯度输出平面 blade，T 结遮挡输出点序不等式。
- *场景分割层*：物体实例输出凸包或椭球 blade，透明层输出 blade 线性叠加。
- *知觉组织层*：轮廓片段输出其圆 blade，平行/正交关系输出 blade 等式约束。
- *单眼特征层*：不变——Gabor 相位/方向与 2D GA 偶子代数同构，无额外接口。

= 最小生成模型与误差传播

前述神经机制——层级处理、预测编码与稀疏编码——在数学上可统一为一个核心原则：每一层视觉模块都是一个最小生成模型，仅传播当前层无法预测的残差信号。形式化地，第 $k$ 层在时刻 $t$ 的状态更新为：

$ F_t^(k) = G^(k)(F_(t-1)^(k), F_t^((k-1)), hat(F)_(t-1)^((k+1))) + epsilon_t^(k) $

其中 $G^(k)(dot)$ 是该层的生成模型——它综合上一时刻自身状态 $F_(t-1)^(k)$、低层前馈输入 $F_t^((k-1))$ 和高层自上而下的预测 $hat(F)_(t-1)^((k+1))$，生成当前状态的预测。$epsilon_t^(k)$ 是预测残差——仅当输入与预测不一致时才非零。

*最小性*来自 Barlow 高效编码假说：系统应以最小冗余表征输入。若生成模型足够好，残差 $epsilon_t^(k)$ 是稀疏的——绝大多数时刻为零，仅在"意外"处（物体边界、运动突变、光照剧变）才需要向上传播。这一点将注意力、预测编码和稀疏编码统一为同一机制的三重表达：注意即残差加权、预测即生成模型输出、稀疏即高效编码目标。

将本架构的八个模块映射到这一框架下：

#academic-table(
  [],
  columns: 4,
  format: "ieee",
  header: ([模块], [生成模型 $G^(k)$], [预测目标], [残差 $epsilon_t^(k)$]),
  (
    [单眼特征层],
    [高层特征预测 + 光学先验],
    [Gabor 响应幅度与相位],
    [显著性图 → 注意加权],
    [知觉组织层],
    [关联场 + Gestalt 先验],
    [轮廓连续性、纹理同质性],
    [边界断裂、区域不一致 → 回传],
    [单目深度线索],
    [shape-from-shading + 物理/几何先验],
    [似然 $P(I mid S)$],
    [似然-先验不一致 → 降权],
    [双眼视差估计],
    [极线几何 + 平滑先验],
    [视差图],
    [匹配代价 → 可靠性权重],
    [深度融合层],
    [多线索贝叶斯生成 $P(I mid S) P(S)$],
    [2.5D 深度图],
    [后验方差 → 注意分配],
    [时空累积层],
    [Kalman 状态转移 $F_t = A F_(t-1) + w_t$],
    [下一帧融合深度],
    [仅编码预测误差（稀疏）],
    [三维重建层],
    [几何/语义先验 + 类别形状],
    [完整三维场景],
    [高层残差 → 反向传播到 fusion/seg/feat],
    [场景分割层],
    [类别模板 + 分层先验],
    [物体边界与语义标签],
    [与深度预测不一致 → 双向修正],
  ),
)

表中关键洞察在于：*每条通路都是一个生成模型*。单目深度线索生成深度似然，双眼视差生成视差图，时空累积生成下一帧预测——它们的输出不是直接送到下一层，而是作为*可被推翻的假设*（$hat(F)$）传入融合层。融合层以贝叶斯方式加权整合各通路的预测，仅当预测与观测（或通路间）不一致时，残差 $epsilon$ 才非零并向上传播。

这一框架直接解释了本架构中最反直觉的设计决策——*双目通路是可选而非必需的*。在生成模型视角下，缺少双目输入并非"信息缺失"，而是该通路的生成模型在给定条件下无法产生可靠预测，其可靠性权重自然降为零。系统退化为纯单目生成——精度下降但结构不坍塌。这比"硬编码一条兜底通路"更深刻：它意味着系统并非在两条通路间切换，而是*始终*以同一生成-残差机制运行，只是双目通路的贡献随输入条件自然增减。

== 先验作为生成模型的内建结构

六类视觉先验并非在生成模型之外施加的独立约束，而是 $G^(k)$ 的固有组成部分——修改先验就是修改生成模型的结构。

- *光学先验* $→$ 似然函数的参数化。光源上方先验编码为 shape-from-shading 生成器中光源方向的固定偏置，白平衡与 Retinex 编码为特征层生成模型的归一化参数。这些不是可学习的权重，而是硬编码在 $G^((1))$ 和 $G^((4))$ 中的不变结构。
- *几何与结构先验* $→$ 生成模型输出的正则化项。平滑性施加 $norm("grad"  G)^2$ 惩罚，曼哈顿世界将输出平面 blade 约束为三组正交方向，对称补全将遮挡半侧建模为可见半侧经 motor 反射的几何积。这些先验的作用域是 $G^((5))$ 和 $G^((7))$。
- *物理先验* $→$ 似然函数 $P(I mid S)$ 的形式。遮挡 T 结编码为深度顺序的不等式约束（二值先验），大气透视编码为距离的指数衰减函数（连续先验），立体视差编码为左右投影的几何关系（确定性先验）——三者分别为二值、连续、确定性的先验注入方式，覆盖了 $G^((3))$、$G^((4))$ 和 $G^((5))$。
- *半透明与分层先验* $→$ 生成模型的输出结构。$G^((text("seg")))$ 输出多个 blade $L_1, L_2, dots$ 而非单一分割，约束 $I approx L_1 + L_2$（Metelli 混合律）。软分配即后验概率 $P(text("pixel") in L_i)$ 而非 $"argmax"$——这直接改变生成模型的输出维度。
- *运动与时间先验* $→$ 状态转移矩阵 $A$ 的结构。低速先验使 $A approx I$（状态接近上一帧），加速度限制约束 $norm(A_t - A_(t-1))$ 小。这些先验不改变 Kalman 滤波的形式，只改变 $A$ 的先验分布 $P(A)$。
- *语义与经验先验* $→$ 自上而下的预测 $hat(F)$。高层生成模型 $G^((7))$ 的类别模板和 $G^((text("seg")))$ 的熟悉大小，以预测 $hat(F)_((text("recon")))$ 的形式注入低层——贝叶斯形式中是先验分布 $P(S)$，预测编码中是高层生成的预测信号，两种语言描述同一件事。

这六类先验的注入方式覆盖了生成模型的全部可定义组件：似然函数的形式（光学、物理）、输出的正则化（几何）、输出的结构（分层）、状态转移的参数先验（运动）、高层预测的生成参数（语义）。没有哪个先验需要"外部添加"——它们要么是 $G$ 的结构，要么是 $P(S)$ 的形式。

#bibliography("references.bib", title: "参考文献")
