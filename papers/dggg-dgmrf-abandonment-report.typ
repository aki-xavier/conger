#import "./template.typ": *
#show: template
#show: tmp_page
#import "@preview/mmdr:0.2.2": mermaid
#import "@preview/akatable:0.1.0": academic-table
#print_title("图像软聚类：两份废弃方案的技术复盘")

#quote(block: true)[
  向导师汇报：为什么 DGGG 方案和 DG-MRF
  方案被放弃、过程中尝试了哪些失败的想法、以及哪些组件被保留。
]

= 问题定义

目标：从 RGB 图像到逐像素软隶属度图的解析管线。四个硬约束———

+ #strong[无训练数据]：不能依赖标注
+ #strong[无梯度优化]：全部计算必须是闭式代数
+ #strong[可审计]：每个模块的输入输出可单独检查，失效时可定位
+ #strong[可复用]：组件不绑定特定任务

在此约束下，研究沿信号分析 → 特征工程 →
聚类推理三条轴线递进，经历了约两个月的迭代。

= 探索阶段

在 DGGG
聚类方案成型之前，有约五周的信号分析与特征探索期。这些工作虽未直接产出聚类方案，但确立了后续所有方案共享的特征基础，并排除了多条错误路径。

== 九种合成信号的构造

构造了完整的不连续性分类体系（信号
01--09），覆盖六种纹理边界类型。这组信号在后续所有特征迭代中充当标准测试集———每个新的特征设计都先在这九种信号上验证行为正确性，再进入聚类管线。

#academic-table(
  [],
  format: "ieee",
  columns: 5,
  align: (auto, auto, auto, auto, auto),
  header: ([信号], [类型], [左侧], [右侧], [验证目标]),

  (
    [01],
    [边→平],
    [纯黑],
    [纯白],
    [亮度阶跃（基线）],
    [02],
    [边→斜坡],
    [纯黑],
    [线性斜坡],
    [梯度不连续性],
    [03],
    [纹理→平滑],
    [8 px 光栅],
    [均匀灰],
    [纹理消失],
    [04],
    [噪声],
    [—],
    [—],
    [宽带噪声基线],
    [05],
    [方向特异性],
    [0° 光栅],
    [均匀灰],
    [仅 θ=0° 触发不连续],
    [06],
    [平滑→纹理],
    [均匀灰],
    [8 px 光栅],
    [类型 B 反转],
    [07],
    [同向不同波长],
    [16 px 光栅],
    [8 px 光栅],
    [仅质心和滚降跳变],
    [08],
    [明→暗平滑],
    [均匀亮],
    [均匀暗],
    [纯光照变化],
    [09],
    [噪声→纹理],
    [高斯噪声],
    [8 px 光栅],
    [全六指标跳变],
  ),
)

#figure(image("./artifacts/response_freq_signal01.png", alt: "九种信号的频谱度量剖面"), caption: [
  单位阶跃信号的频谱度量剖面
])

== 边缘检测的六次迭代（全部被替换）

在确定以 Pb
半盘检测器作为最终边界通道之前，先后尝试了六种边缘指标，每种都在合成信号上构造了反例后被替换：

#academic-table(
  [],
  format: "ieee",
  columns: 3,
  align: (auto, auto, auto),
  header: ([迭代], [方法], [失败原因]),
  (
    [1],
    [Riesz 方向投影],
    [方向分辨率不足，0° vs 90° 不可分],
    [2],
    [相位一致性],
    [频域插值引入环形伪影],
    [3],
    [边缘置信度],
    [合成正确，真实图像 Pb 噪声过高],
    [4],
    [纹理连续性指标],
    [信号 05 正确，跨信号泛化失败],
    [5],
    [纹理指示器 +
      边缘指示器融合],
    [双通道融合的门控阈值人工依赖过重],
    [6],
    [f² 加权能量质心],
    [合成断裂边界上数值不稳定],
  ),
)

最终收敛于 Pb 半盘检测器（Martin et
al.~2004）：以"跨候选边界的纹理分布变化"替代逐点梯度———不度量局部变化率，而度量两侧区域的整体差异。信号
01--09 的每一类不连续性都对应于半盘两侧统计量的跳变。

== 频谱度量的三次重构

聚类特征的核心：频谱形状，经过了三次代数重构才定型：

#academic-table(
  [],
  format: "ieee",
  columns: 3,
  align: (auto, auto, auto),
  header: ([版本], [特征构成], [问题]),
  (
    [v1],
    [原始 Gabor 能量（S×M 维）],
    [40 维，纯幅度，光照敏感],
    [v2],
    [方向池化 + 对数压缩],
    [降维但丢失方向信息],
    [v3],
    [增益控制谱形状 + log 幅度 + 5 谱矩 + 圆方差 +
      可选方向组],
    [定型：光照不变 + 方向按需启用],
  ),
)

增益控制的引入是 v3 相对于 v2 的关键差异。shaded split 场景上，v2的对数压缩将 100× 能量范围压缩为约 4.6 nat偏移，但偏移本身仍可检测并劫持聚类。v3的除法归一化将该偏移精确归零：同一纹理在亮处和暗处的频谱形状逐维相等。

== 谱聚类的失败

在 DGGG 之前，尝试了 Ng--Jordan--Weiss 谱聚类：以 Gabor特征间的方向感知距离构建相似图，取拉普拉斯的前 K 个特征向量做k-means。失败在两个层面：

- #strong[距离度量]：方向感知距离需圆统计，谱聚类的高斯核不兼容
- #strong[空间位置实验]：在特征向量中附加像素坐标短暂提升了空间连贯性，但破坏了簇的旋转不变性；移除坐标后聚类回归无空间上下文的随机状态

谱聚类实验是后续聚类路线的关键决策点：它证明了#emph[任何不包含空间先验的聚类方法都面临同一个死胡同]。空间先验不是"更好"，而是"必须"。谱聚类的拉普拉斯特征向量分解和后续DGGG的局部后验最大化面临同样的局限，没有空间上下文时，特征空间中的噪声与真实类别差异无法区分。

== 特征工程中被否决的尝试

在特征向量定型过程中，以下特征被尝试后移除（commit 历史有完整代码）：

#academic-table(
  [],
  format: "ieee",
  columns: 4,
  align: (auto, auto, auto, auto),
  header: ([特征], [加入 commit], [移除 commit], [移除原因]),
  (
    [空间坐标附加到特征向量],
    [91029b9],
    [d25992d],
    [破坏旋转不变性],
    [DC 响应作为独立特征],
    [9bb4628],
    [271f4b9],
    [与 log
      幅度高度相关，冗余],
    [方向能量圆统计（复指数合成子）],
    [ef430db],
    [70cd433],
    [v1
      实现与增益控制冲突],
    [空间方差 + 长程相干性特征],
    [9150133],
    [cb0dc2c],
    [在 32×32
      测试图像上统计不稳定],
    [旋转不变特征（谱矩组合）],
    [25cbf89],
    [—],
    [部分保留，部分被谱矩替代],
  ),
)

方向能量圆统计的第二次加入（v3 版本中的 $\(C_s\,S_s\)$
合成子）是在增益控制定型之后，解决了 v1 版本中与幅度耦合的问题。

= 方案一：DGGG（Dual-Gabor Gaussian GMM）

== 做法

对每个像素提取 Gabor 频谱特征向量（~26 维），拟合全协方差 GMM（$K$
个高斯分量），以每个像素的后验概率作为软隶属度：

$
  Q_(i k) = frac(pi_k upright("N")\(f_i divides mu_k\,Sigma_k\), sum_l pi_l upright("N")\(f_i divides mu_l\,Sigma_l\)) .
$

#strong[没有任何空间先验]———每个像素独立决策，邻域之间不交换信息。

== 数据

#academic-table(
  [],
  format: "ieee",
  columns: 3,
  align: (auto, auto, auto),
  header: ([场景], [精度], [备注]),
  (
    [quadrants（4 象限复合纹理）],
    [85.7%],
    [特征空间分离好],
    [vertical split（双光栅直边界）],
    [98.9%],
    [波长差异足够大],
    [chroma split（等亮度红蓝）],
    [100%],
    [色相在 HSL 中天然分离],
    [orientation 0°/90°],
    [*54.1%*],
    [随机水平———致命],
    [shaded split（100×
      光照渐变）],
    [*78.8%*],
    [幅度差异劫持了聚类],
  ),
)

== 三个致命失效模式

#strong[方向判别陷阱（54.1%）]：8 px 0° 光栅 vs 8 px 90°
光栅。两者的尺度能量分布完全一致，差异仅在方向分量中，而 DGGG
的高斯协方差无法正确建模圆形分布（$2 theta$ 在单位圆上）。不启用方向组则
54.1%（随机水平）；启用后被高斯拟合偏差拖累。

#strong[噪声散点]：在 quadrants 上加 $sigma = 0.15$
高斯噪声后，平坦区域的特征向量在空间中散布，散布方向与三个纹理象限的分布重叠。无邻域信息时，散布像素被随机分配，产生椒盐噪声块。

#strong[边界混淆]：边界带像素（±2 px）的 Gabor
特征被两侧纹理共同污染———Gabor 包络跨边界拖尾。DGGG
将这些污染特征解释为"新的混合纹理"而非"边界上的不确定像素"，结果是边界带像素被随机分配到无关簇中。

== 弃用理由

不是精度不够，而是#strong[无空间上下文时，局部特征噪声与真实类别差异无法区分]。三个失效模式指向同一个根因：像素之间必须交换信息。

此外，DGGG 与下游的实时三维重建任务存在结构性不兼容：DGGG
的逐像素独立决策不产生任何边界信息或区域先验———聚类输出对深度估计的贡献为零，但聚类本身占用了计算预算。在实时约束下，这是一个无法优化的结构性浪费：不是"DGGG
太慢"，而是"DGGG
的输出格式不携带任何深度管线需要的信号"（无边界通道、无区域先验、无聚类熵）。即使
DGGG 精度完美，它对下游任务的贡献仍然是零。

== 被保留的

DGGG 的 EM 拟合（在 $K$
个分量的均值附近找到初始化起点）被保留为后续方案的簇原型初始化器。全协方差矩阵被弃用———后续方案只继承均值位置。

= 方案二：DG-MRF（双通路 Gabor 软 MRF）

在 DGGG 一元势上叠加成对马尔可夫随机场，目标函数显式分离：

$
  E\(Q\)= underbrace(sum_i sum_k Q_(i k) dot.op U_(i k), upright("数据项")) + underbrace(beta sum_(\(i\,j\)) w_(i j) dot.op\["argmax"\(Q_i\)eq.not "argmax"\(Q_j\)\], upright("平滑项")) .
$

$w_(i j)$ 由 Pb 边界通道调制。双通路：亮度通路和色度通路独立运行
MRF，后验乘积汇合。色度编码为 $S e^(i H)$。

== 三个额外超参数

#academic-table(
  [],
  format: "ieee",
  columns: 3,
  align: (auto, auto, auto),
  header: ([参数], [语义], [标定方式]),
  (
    [$beta$],
    ["一票邻域一致值多少 nat 的特征证据"],
    [人工网格搜索],
    [$T$],
    [softmax 温度],
    [二分搜索（~100--200 步 × 6 轮）],
    [边界门控],
    [Pb > 0.5 像素的精度衰减 + 事后投票],
    [两阶段手工流程],
  ),
)

== 改进

#academic-table(
  [],
  format: "ieee",
  columns: 5,
  align: (auto, auto, auto, auto, auto),
  header: ([场景], [DGGG], [DG-MRF], [提升], [归因]),
  (
    [quadrants],
    [85.7%],
    [93.1%],
    [+7.4],
    [横向亲和度],
    [orientation],
    [54.1%],
    [97.9%],
    [+43.8],
    [方向组 + 空间平滑],
    [shaded
      split],
    [78.8%（无增益控制）],
    [93.8%],
    [+15.0],
    [增益控制（与 MRF
      无关，DGGG+增益控制亦达 ~93.8%）],
    [noise σ=0.15],
    [—],
    [90.5%],
    [—],
    [空间平滑压制散点],
    [thin stripe],
    [—],
    [93.5%],
    [—],
    [均值场扩散入窄条],
    [curved],
    [—],
    [96.8%],
    [—],
    [关联场核横向耦合],
  ),
)

#figure(image("./artifacts/soft_clustering_cases.png", alt: "七种标准测试场景的软聚类结果"), caption: [
  七种标准测试场景的软聚类结果
])

空间先验确实解决了 DGGG 的全部三个失效模式。

== 三个新问题

#strong[$beta$ 的语义标定]：$beta$ 回答"一票邻域一致值多少 nat
的特征证据"。依赖两件无法预知的事———簇间距离量级（2--3
数量级跨场景差异）和 Pb 响应分布。需逐场景网格搜索。

#strong[$T$ 的二分搜索成本]：6 轮二分 × 每轮 ~100--200 步均值场 =
推理成本 ×6。且 $T$ 耦合于 $beta$———改一个必须重搜另一个。

#strong[边界两步后处理]：Pb > 0.5 的像素先降 pairwise
权重，收敛后基于无包络点特征投票重判。硬阈值（0.49 vs 0.51
天差地别）、两遍语义不一致、流程不可解释。

== 过程中尝试并否决的方向

在 DG-MRF
框架定型前后，以下改进方向被逐一实验、测量、否决。每个都死于测量而非直觉。

=== 推理算法层面

#academic-table(
  [],
  format: "ieee",
  columns: 3,
  align: (auto, auto, auto),
  header: ([尝试], [结果], [死因]),
  (
    [HMRF-EM],
    [61--66%],
    [硬分配丢失边界不确定性；orientation 失败于 DC
      伪影构成的高似然+高相干局部最优],
    [均值场 tol 早停],
    [校准恶化],
    [noise ECE
      0.059→0.086———低置信像素在早停时未充分收敛],
    [Pyramid coarse-to-fine],
    [87.2%→48.5%],
    [粗尺度锁错后，细尺度邻域一致性将错误放大到全图———路径依赖的灾难性失效],
  ),
)

=== 特征与度量层面

#academic-table(
  [],
  format: "ieee",
  columns: 3,
  align: (auto, auto, auto),
  header: ([尝试], [结果], [死因]),
  (
    [特征显著性加权],
    [53%],
    [高斯边缘分布让所有维保持显著（伪影像素是尾值）；KDE
      边缘分布让所有显著性塌缩到零。真正的改善来自对角协方差本身（全协方差
      67.1%→对角 89.7%），不是显著性],
    [Cauchy--Crofton
      metrication],
    [无改善],
    [均值场推理在图权重已逐对变化时，网格偏差在进入推理前已被稀释],
    [Laws TEM],
    [精度显著下降],
    [固定核的空间—频率选择性远低于倍频程
      Gabor 组],
  ),
)

=== 边界与区域层面

#academic-table(
  [],
  format: "ieee",
  columns: 3,
  align: (auto, auto, auto),
  header: ([尝试], [结果], [死因]),
  (
    [Boundary-gated unary],
    [薄条
      \+1.3，象限 −1.3，噪声
      −1.6],
    [非一致改善———移除正确局部证据的地方反而恶化],
    [Junction bimod 门控],
    [精度
      −1.0%],
    [交界像素 \<0.5%，额外逻辑的误判多于纠错],
    [区域编组],
    [两种死法],
    [① Pb
      被光照劫持———阴影环带即边界，分量 = 环带；② 低对比度轮廓 Pb
      断裂———球与背景融合为同一分量],
  ),
)

#figure(
  image("./artifacts/region_grouping.png", alt: "区域编组的失败：左球（Pb 被光照劫持），右球（Pb 轮廓断裂）"),
  caption: [
    区域编组的失败：左球（Pb 被光照劫持），右球（Pb 轮廓断裂）
  ],
)

=== 模型选择与自动化

#academic-table(
  [],
  format: "ieee",
  columns: 3,
  align: (auto, auto, auto),
  header: ([尝试], [结果], [死因]),
  (
    [自动 K 选择],
    [全部失败],
    [BIC 在 D>20
      过度惩罚维度；联合能量被每像素平移破坏；中位 gap
      的峰值阈值跨场景不重叠；固定温度置信度在噪声场景低报 K],
    [色度纹理无条件启用],
    [自然图像误选],
    [加 15%
      相对相干性增益门槛后稳健（实测两侧 gap +7% vs +39%）],
    [DGGG 全像素拟合],
    [精度无差异],
    [max_samples=16384 已充分确定 K·D²
      协方差参数],
  ),
)

=== 工程参数的隐性调参成本

以下参数虽最终固定，但选择过程依赖多次试验：

- $epsilon$ 下限：图像总能量均值的 1%，防止平坦像素的频谱形状被噪声放大
- DGGG n_init：小图 10 次重启，大图子采样 3
  次———重启彩票在子采样下无差异
- Pb edge_scale：从 0.3 降至 0.25（commit 95fd324）———单次试验决策
- 色度纹理 coh margin：15%———左侧 gap +7%（不启用更好）vs 右侧
  \+39%（必须启用），间距大，阈值稳健

#figure(image("./artifacts/real_images.png", alt: "真实图像定性验证"), caption: [
  真实图像定性验证
])

== 根因

这三个问题的共同根因是
#strong[$E = E_(upright("data")) + beta E_(upright("smooth"))$
  的分离形式]。只要数据项和平滑项是独立的代数项：

- 需要 $beta$ 宣告两者的相对重要性
- 需要 $T$ 控制软硬程度
- 需要在边界处切断平滑

且 $beta$
是全局标量———但平滑需求在空间上是异质的：纹理内部需强平滑，边界处需零平滑，阴影边缘需中等平滑。一个全局标量无法适配。

== 弃用理由

DG-MRF
的三参数不是"标定不足"的问题———它们是#strong[目标函数分离这一设计决策的必然产物]。12
个被否决的改进方向覆盖了推理算法、特征权值、度量、边界处理、模型选择、多尺度和色度策略的全部维度。同框架内继续改进的边际收益为零———剩下的问题（$beta$
全局标量 vs 异质平滑需求）是代数结构本身嵌入的，无法在分离形式下修复。

此外，DG-MRF 与下游的实时三维重建任务存在结构性冲突。在 640×480
分辨率的端到端热帧中，DG-MRF 聚类占 37% 计算成本（2.5
秒），但其输出的软隶属度 $Q$ 并不流入深度计算———深度管线只消费 M04
的原始 Gabor 复响应，不消费聚类结果。聚类花了 2.5
秒计算一个对深度质量边际贡献为零的量。这不是优化问题———即使把 DG-MRF
的聚类速度提升 10 倍，它仍然占据 250 ms
的热帧预算，而它的输出仍然不贡献深度精度。DG-MRF
的架构把聚类放在深度计算的前提条件位置上，但聚类的输出格式（软隶属度图）与深度管线的输入格式（Gabor
复响应）不匹配———这是一个信号流拓扑错误，不是性能优化能解决的。

== 被保留的

#academic-table(
  [],
  format: "ieee",
  columns: 3,
  align: (auto, auto, auto),
  header: ([组件], [状态], [理由]),
  (
    [Pb 半盘边界检测器],
    [保留],
    [纯前端，不依赖 MRF],
    [增益控制除法归一化],
    [保留],
    [光照不变性是特征层的独立贡献],
    [DGGG EM 拟合],
    [保留为初始化器（降级）],
    [原型位置的初始化仍然有价值],
    [圆统计 $2 theta$ 编码],
    [保留],
    [],
    [色度复数编码 $S e^(i H)$],
    [保留],
    [],
    [特征组自动选择（相干性判据）],
    [保留],
    [],
    [均值场迭代],
    [弃用],
    [],
    [Potts 平滑项],
    [弃用],
    [],
    [温度二分搜索],
    [弃用],
    [],
    [边界门控 + 投票],
    [弃用],
    [],
  ),
)

DG-MRF 的弃用结论是明确的———$E = E_(d a t a) + beta E_(s m o o t h)$
的分离形式是根本限制。但"否定
DG-MRF"只告诉你哪条路不通，不告诉你该往哪走。#strong[在四个硬约束（无训练数据、无梯度优化、可审计、可复用）下，如果不分离数据项和平滑项，能怎么办？]

有四个显而易见的备选路径———每条都被认真考虑过，但每条都在某个硬约束上碰壁。

#strong[路径 A：在 DG-MRF 框架内自动标定 $beta$ 和 $T$。]
用贝叶斯优化在每类场景上离线跑一次，把最优参数硬编码为查找表，在线推理时按场景类型查表。这条路的死法不是"做不到"———是可以做到的———而是#strong[不值得]。因为
4.4 节列出的 12
个被否决方向已经证明了同框架内改进的边际收益为零：自动标定只是把手工网格搜索换成算法网格搜索，不解决
$beta$
是全局标量无法适配异质平滑需求的根本矛盾。一个在边界处需要零平滑、在纹理内部需强平滑、在阴影边缘需中等平滑的场景———一个全局
$beta$
无论怎么优化都是错的。这不是标定精度问题，是"全局标量"这个代数类型与"异质需求"之间的类型不匹配。

#strong[路径 B：让 $beta$ 变成空间变化的。]
给定边界通道和一阶特征梯度，设计一个函数 $f$ 在边界处输出
$beta approx 0$、在平滑区域输出
$beta > 0$、在阴影边缘输出中等值。这条路在代数上是可行的———事实上后续方案中的
$S_(i j)$ 和 $lambda_i$ 的边界衰减做的是完全相同的事。但死因在于：在
DG-MRF 的分离形式中，$f$ 本身变成了新的超参数源。$f$
需要至少两个参数———边界衰减斜率、特征梯度带宽———而这两个参数同样需要标定。用
$f$ 替代 $beta$ 只是把标定负担从 $beta$ 转移到 $f$
的参数上，没有减少参数总量———换汤不换药。

#strong[路径 C：换一个框架但保持数据+平滑分离。]
条件随机场（CRF）、图割、变分水平集———所有这些方法的目标函数都可以写成
$E_(d a t a) + lambda E_(p r i o r)$。分离形式不是 DG-MRF
的专属问题，它是在概率图模型中普遍存在的结构。"换个框架"不解决这个问题———只要分离存在，$l a m b d a$
就必须标定。而且 CRF
和图割的推理成本（全图消息传递、最大流最小割）在像素级密度上比均值场更高———DG-MRF
已经够慢了，换框架只会更慢。

= 从 DG-MRF 到最小生成模型：当前状态与后续计划

== 为什么选最小生成模型

四条路径在同一个难点上碰壁———#strong[必须存在一个框架，在这个框架里，空间上下文不是外加的附加项，而是目标函数自身的代数结构产生的必然结果。]
只有在这种情况下，空间平滑才能从"需要人类判断强度的惩罚"变成"由代数恒等式自动确定的传播"，参数标定才能从"人类指定数值"变成"由数据统计实例化"。

这个要求指向了一类特殊的目标函数：$sum_i ell\(f_i\,hat(f)_i\(Q\,P\)\)$———对每个像素，定义该像素的生成模型预测
$hat(f)_i$，目标是最小化全体预测误差。在这个形式下：

- #strong[不存在分离]———目标函数只有一项。不存在"数据项"和"平滑项"的区分，因此不存在宣告两者相对重要性的
  $beta$。
- #strong[平滑来自误差传播的结构]———如果邻域像素 $j$ 的误差 $e_j$
  通过精度加权的梯度注入像素 $i$ 的更新
  $tilde(g)_(i k) = p_k^top Lambda_i e_i + alpha sum_j S_(i j) p_k^top Lambda_j e_j$，那么
  $i$
  的隶属度更新同时响应#emph[自己的]未解释残差和#emph[邻域的]未解释残差。这不是"平滑操作"，而是"协同解释"———多个相邻像素的生成模型协同调整以共同消除残差。
- #strong[精度来自数据统计]———$Lambda_i$ 的值是 $"Var"$ 和 $"bmap"$
  的代数函数，不需要人类标定。$S_(i j)$
  的值是特征距离、边界强度和关联场角的乘积，也不需要人类标定。

这类目标函数对应的数学结构被称为"层次生成模型中的精度加权预测误差最小化"———在神经科学文献中称为预测编码（predictive
coding）。其工程对应是：$K$ 个原型向量 $p_k$ 生成特征预测
$hat(f)_i = sum_k Q_(i k) p_k$，精度加权残差 $e_i^top Lambda_i e_i$
驱动隶属度 $Q_(i k)$ 的定点迭代。

这个框架之所以被称为"最小生成模型"，是因为生成部分真的只有 $K$
个原型向量———没有神经网络、没有过完备字典（稀疏编码的几百个基向量）、没有隐空间映射（VAE
的编码器-解码器对）、没有判别器（GAN）。生成的语义是："每个像素的特征是由其所属纹理的原型按隶属度混合而成的"。唯一需要推理的是哪些原型以什么比例混合———而所有空间上下文、精度分配和层次组织都从预测误差在层级间的传播中自动涌现。

== 从三角关系看架构的必然性

5.1节论证的是最小生成模型在聚类问题内部的优越性———单一目标函数、精度由数据统计实例化。但选择这条路的更深层原因在聚类之外：#strong[4.5
  节得出的根因———加性目标函数的分离形式———在聚类内部表现为
  $beta$/$T$/门控三重标定；当视角从单一组件扩展到由相似性聚类、深度估计、时间累积三个组件构成的感知系统时，同一个根因表现为更严重的问题。]
三个组件构成一个三角形，每条边都需要跨组件的信息交换，且交换的货币必须在三条边上语义统一。
前两版方案的失败，表面上是各自的问题，深层是同一个问题———它们的数学结构无法支撑多组件感知系统所需的通货。

#figure(caption: [三角关系：相似性聚类、深度估计、时间累积三者以精度为统一货币进行信息交换])[
  #image("./artifacts/mermaid.png", width: 60%)
]

三条边各自的需求：

- #strong[聚类 ↔ 深度]：聚类向深度输送
  bmap、$H_(e n t)$、表面先验（理论上深度也反向输送遮挡裁决）。交换的不是数值而是置信度———聚类熵的
  0.3 和匹配置信度的 0.3 必须能放进同一个公式里运算，且跨场景含义不变。
- #strong[聚类 ↔ 时间]：隶属度是慢变状态，前一帧的 $Q$
  应成为当前帧的先验。需要的不是"记住上次的答案"，而是一个定义在 $Q$
  上的动力学———当前帧的证据进入时，$Q$ 沿误差减小的方向平滑演化。
- #strong[深度 ↔
    时间]：深度场跨帧积累，高置信测量驱动快速更新，低置信区域靠先验传播———同一状态变量，不同像素以不同速率吸收新证据。

#strong[DGGG 在三条边上全部交白卷]（详见
3.4）：硬标签无法表达"不确定"，没有 $H_(e n t)$
可输出；每帧从零拟合，没有可以演化的状态；输出格式与深度输入格式不匹配。它是只进不出的终端节点。

#strong[DG-MRF 有货币，但币值混乱且每笔兑换都要人工定价。] 边 1
上，$H_(e n t)$ 的数值语义由温度 $T$ 决定———$T$
是二分搜索标定的产物，同样的证据分布在不同标定下产出不同的熵，跨场景的"0.3"没有一致含义。边
2 上暴露得更彻底：ClusterProcess 的时间存活靠"冻结 GMM +
热启动"工程操作，切变检测靠外接的 CUSUM 判据———因为加性目标函数
$E = E_(d a t a) + beta E_(s m o o t h)$
里没有时间项的位置，要加时间耦合就得加第三项
$g a m m a E_(t e m p o r a l)$，而 $gamma$
的语义（"一帧前的隶属度值多少 nat
的当前证据"）又是一个需要标定的汇兑率。#strong[加性结构的根本缺陷：每增加一种组件间关系，就增加一个需要人工标定的权重]———空间平滑要
$beta$，时间耦合要 $gamma$，深度耦合要
$delta$，每个都依赖场景统计、彼此耦合，汇兑成本随关系数量线性增长。

#strong[最小生成模型用一种货币贯通三条边———精度。] 它满足两条公理：

+ #strong[乘法合成]：多来源的置信度以乘积汇合，无需汇兑率。深度融合的
  $L a m b d a = C_(m a t c h) d o t\(1 - H_(e n t)\)d o t\(1 - P_(o c c)\)$———三个因子各自由其数据源独立决定语义，乘起来就是联合置信度。对照
  DG-MRF 的加性结构必须回答"平滑票值多少数据
  nat"。精度相乘，能量相加；相乘不需要汇兑，相加必须汇兑。
+ #strong[数据实例化]：每个精度因子由前端统计自动给定———$C_(m a t c h)$
  来自代价曲面峰度，$H_(e n t)$
  来自校准后验，$lambda_d = 1\/o p\(V a r\)$
  来自特征方差。币值由数据印刷，不由人工定价。

逐边验证：边 1 上，$H_(e n t)$ 的概率语义由 $lambda_i$
的分布保证———软度是"证据可靠度"的直接函数而非温度旋钮的产物，可按字面概率意义跨场景使用。边
2 上，$Q$ 的定点迭代是梯度流 $t a u thin d Q\/d t$
的离散化———前一帧的收敛态在输入变化时自动进入瞬态重新收敛，"状态跨帧存活"是方程的内蕴性质，不是冻结+热启动+CUSUM
三件事的拼凑。边 3 上，深度融合
$Z_t = Z_(t - 1) + L a m b d a\(Z_t^"meas" - Z_(t - 1)\)$ 与 $Q$
更新在代数上是同一个式子：状态 = 先验 + 精度 ×（观测 − 先验）。$Q$
的迭代是它在隶属度空间的实例，$Z$
的滤波是它在深度空间的实例，时间累积是它在时间轴的实例。

这就是为什么选择最小生成模型：#strong[只有在"预测误差 +
  精度"的代数里，聚类、深度、时间三者的信息交换才有统一语义且零标定成本。]
前两版的失败表面上是各自的问题（DGGG 无空间上下文、DG-MRF
超参数标定），深层是同一个问题———它们的数学结构无法支撑多组件感知系统所需的通货。

== 计划方向：第三版方案

两份方案的经验收敛为一条设计原则：#strong[空间上下文不能通过外加惩罚项引入，而必须内嵌在目标函数的精度结构中。]
基于这条原则的第三版方案———以最小生成模型的预测误差最小化统一数据项与平滑项———是下一步的工作方向。从
DGGG 继承原型初始化器、从 DG-MRF 继承前端基础设施，但不继承任何 DG-MRF
的超参数标定方法。所有运转所需的数值均由前端统计实例化。

第三版方案预计可以同时解决 DGGG 和 DG-MRF
与下游管线的结构冲突：相似性聚类被降级为低频正则化通道（1--5
Hz），向下游管线输出三个信号———边界位置（bmap，切断跨物体深度平滑）、聚类熵（$H_(e n t)$，调制深度更新速率）、表面先验（同簇像素倾向共面）———而深度计算在独立的快路径上以每帧频率运行，两者以精度门控异步耦合。

