# conger 研究路线复盘 · 逐页大纲（第一人称）

主题：我是如何从 Riesz-GMM 显式建模，切换到 conger 当前实例级 MixtureSPN 逆渲染。
配套 PPTX：`docs/conger_riesz_gmm_to_mixture_spn.pptx`（16:9, 15 页）。
生成脚本：`tools/build_conger_research_pptx.py`。

---

## 1. 标题页

- **标题**：我是如何从 Riesz-GMM 显式建模，切换到实例级 MixtureSPN 逆渲染
- **署名**：研究路线复盘 · 第一人称 · 2026-08（留空署名）
- **讲述重点**：一句话点题——这是一次「由实测否决驱动的转向复盘」，不是平滑演进，而是两次明确退役（离散码、EM 质心）后收敛到实例级 MixtureSPN。点出四个硬约束（无训练数据 / 无梯度 / 可审计 / 可复用）与当前生成过程路线的对照。
- **证据来源**：git 时间线 `8ed8356 (07-24)` → `aaa43e0 (07-27)` → `97c33d6 (08-11)` → `523b97a` / `54cd2d1 (08-13)`。

## 2. 一句话总览

- **标题**：我把显式从模块移到生成过程
- **讲述重点**：对照两列——旧「我把显式放在模块里：逐模块闭式、手工先验/门控/参数 β·T」；新「我把显式放在生成过程里：单一 Codebook→render，统计规律交给实例级后验」。底部三阶段时间轴（显式建模期 → SPN 分支验证 → 实例级主线）给出整体叙事骨架。
- **证据来源**：`docs/architecture.md §0`（两次退役），git log。

## 3. 我要解的逆渲染问题

- **标题**：我要解的逆渲染问题
- **讲述重点**：输入=左右两张 144×144 立体图，输出=完整 cga.Scene（含光照）。强调不适定性（无穷多场景同图）→ 需要强先验（Helmholtz 无意识推理）。列出 8 个输出自由度（kind / u / v / s / z / hue / lcol / ldir）。我把先验分四层落地，与「不过早 argmax」原则。
- **证据来源**：`docs/prior.md`（先验体系），`docs/architecture.md §1`。

## 4. 我现在的架构总图

- **标题**：我现在的架构总图
- **讲述重点**：八节点两行流——立体图 → Riesz 11 通道(V≈228K) → PCA 白化(D≤N−1) → MixtureSPN(逐 kind/K=N/无 EM) → 条件期望/后验 → 渲染残差精炼(top-k kind×54 外观) → StructuredHypothesis → cga.Scene。讲清「白化使对角高斯 ≡ 原空间全协方差」和「渲染残差精炼是外观辨识主源」两个关键点。
- **证据来源**：`docs/architecture.md §1`、`§5`（模块结构）。

## 5. 旧路线：我当初显式建模了什么

- **标题**：旧路线：我当初显式建模了什么
- **讲述重点**：七模块链（知觉组织→分割/BN→单目深度→双目视差→深度融合→Motor-EKF→CGA 重建）。**两阶段显式管线**：① 07-27 (`aaa43e0`) Gabor-GMM 软聚类（DGGG / DG-MRF）；② 08-02 (`3a344b4`) Riesz 替代 Gabor → Riesz + VB-GMM + CGA 显式管线。逐模块闭式、手工先验/门控、参数 β·T；无训练数据、无梯度优化。底部旧路线关键提交时间轴（注意 07-27 是 Gabor-GMM，Riesz 08-02 才引入，不要把 aaa43e0 直接称 Riesz）。
- **证据来源**：`aaa43e0` (Gabor-GMM) + `3a344b4` (Riesz 引入)；`b52ac7a`/`cbc9fa8`/`43416c8`。

## 6. 我先保留了什么（跨阶段合成）

- **标题**：我先保留了什么（跨阶段合成）
- **讲述重点**：分两组，不声称全部来自 aaa43e0：
  - **07-27 报告明确保留（Gabor 时代）**：Pb 半盘检测器 / 增益控制 / 2θ 圆统计 / 复数色相 S·e^{iH} / 合成信号测试（9 信号）/ 聚类初始化；
  - **后续证明继续有效**：Riesz 前端（08-02 3a344b4 引入，替代 Gabor）/ CGA 场景表示 / 双目几何与前景权重。
  我的判断：失败的只是「聚类+手工先验」的显式推理层，前端谱表示（Gabor→Riesz）与几何表示被 SPN 路线继承。
- **证据来源**：`aaa43e0` (07-27 Gabor) + `3a344b4` (08-02 Riesz) + `docs/architecture.md §2.1`。

## 7. 失败证据 I：我怎么判断这不是参数问题

- **标题**：失败证据 I：我怎么判断这不是参数问题
- **讲述重点**：原生条形图对比——DGGG orientation 54.1%（随机水平，致命）/ shaded 78.8%；DG-MRF 把 orientation 拉到 97.9%、shaded 93.8%，但引入 β/T/门控三参数标定（逐场景网格搜索、二分 ×6 轮、硬阈值 0.49/0.51）。12 路改进全部否决（HMRF-EM 61–66%、pyramid 87.2→48.5、显著性 53%）。我的判断：不是参数没调好，是手工先验本身不可标定。
- **证据来源**：`aaa43e0`（DGGG/DG-MRF 数值表）。

## 8. 失败证据 II：我为什么转向（拓扑白卷）

- **标题**：失败证据 II：我为什么转向（拓扑白卷）
- **讲述重点**：三条否决证据——① 聚类占 640×480 热帧 37% / 2.5s，但软隶属度 Q 不流入深度（拓扑错误，非性能问题）；② 单目深度 iBims/NYU Spearman≈0；③ 遮挡序 iBims 0.444≈随机、修正后 0.419、T 结假阳性 76%。我的判断：手工先验/显式推理层产出与下游脱节 → 这就是我转向「生成过程显式」的直接动因。
- **证据来源**：`aaa43e0`；`fffc06a`（Spearman≈0）；`05ac7f0`（0.444→0.419 / T结 76%）。

## 9. 我的决定性实验

- **标题**：我的决定性实验：SPN 场景码 0.970
- **讲述重点**：大数字 0.970。N=2000 场景码整体 0.970 / kind 1.0 / gx 0.99 / gy 0.98 / size 1.0；场景码 288 = kind3 × 8×6 网格 × 2 尺寸（枚举后验 argmax）。我看到「渲染→特征→生成模型后验」能端到端反推场景因子；于是我认定生成模型后验方向可行，SPN 成为主线候选。
- **证据来源**：`97c33d6 (08-11)`（288 = N_KIND × N_GX × N_GY × N_SIZE）。

## 10. 我为什么退役离散码

- **标题**：我为什么退役离散码
- **讲述重点**：连续化验证 N=4000 K=64——kind 0.897（白化 1-NN 上限 0.94）、u/v RMSE 6.6px R²≈0.90、s 0.17 / z 0.44（s/z 弱是熟悉尺寸歧义物理）。我的理由：连续物理量的离散化只是后验求积，不带来信息增益，所以我退役它。后续：tied 方差/收缩已在连续化初版稳定似然；实例级则在下一步「退役质心」时登场。
- **证据来源**：`523b97a (08-13)`；`docs/architecture.md §0`。

## 11. 我为什么退役 EM / 质心

- **标题**：我为什么退役 EM / 质心
- **讲述重点**：对比数字——EM/质心压缩 u R² 0.50 vs 实例级 ≈0.90（全量 1296 u/v≈0.93、RMSE 5.4/5.2px）。**EM 五条退化通道**：目标 razor 门控 / 死分量均值爆炸 90× / 方差无上限（只活 19/216）/ 权重死亡 log_w≈−20 / nk≈3 方差噪声 ±115 nats。口径：`architecture.md §2.2` 用 u≈0.90；`54cd2d1` commit message / 全量口径 u/v≈0.93。我的教训：EM/质心压缩是大数据优化；数据均匀采样 ⟹ 均匀权重是正确先验。
- **证据来源**：`54cd2d1 (08-13)`；`docs/architecture.md §2.2`。

## 12. 我现在把显式性放在哪里

- **标题**：我现在把显式性放在哪里
- **讲述重点**：kind 判别修复链（0.47→0.47→0.47→0.68→✓ 实例级），对应增益控制抹色相幅度 / 对角高斯重复计票 / PCA 白化 / 方差收缩四个坑。实例级组装四要素（逐 kind 分层 / K=N 实例块 / 类内 tied 方差 / 均匀权重）。我的显式 = 生成过程 + 结构契约；其余（统计规律、歧义）全进后验。预测语义：连续=分层核回归，离散=后验分类。
- **证据来源**：`docs/architecture.md §2.1`、`§1`。

## 13. 当前基准（插值/外推）

- **标题**：插值 / 外推 (N=1296, kind_topk=3)
- **讲述重点**：两表对比。插值 u/v 0.930/0.945、s 0.508、z 0.831、kind 0.753、hue 1.000、lcol 0.994、ldir 0.895；外推 u/v 0.949/0.953、s/z 0.922/0.956、kind 0.617、hue 0.981、lcol 0.880、ldir 0.772。底部强调渲染残差精炼贡献：lcol/ldir 0.457/0.367 → 0.994/0.895。附 u,v 散点与重建样例图。
- **证据来源**：`docs/architecture.md §1`；`artifacts/inverse_scatter.png` / `inverse_recon.png`。

## 14. 方法论回声：纹理/roughness

- **标题**：方法论回声：我再次验证纹理 / roughness
- **讲述重点**：11 张图不是结构/统计正交轴，而是同一 p_s=e_s/Σe 的聚合；真正分界=能量(log_mag) vs 形状(slope/moments)。E7：log_mag 对比度 1.00、slope/moments 0.00、slope 对 β 单调。主线实测纹理类型 **tex 0.50 / 0.46 > chance 0.33**；roughness 二阶弱信号被淹没 → 固定 0.55、只保留纹理类型因子。呼应「实测驱动、诚实否决」。
- **证据来源**：`docs/architecture.md §2.3`。

## 15. 复盘：我带走的三条原则与下一步

- **标题**：复盘：我带走的三条原则与下一步
- **讲述重点**（三条第一人称原则）：
  1. **失败要可证伪** —— 每个机制给出可测反例，否决靠数据不靠直觉；
  2. **保留有效先验** —— 信号前端/几何表示留下，只废掉手工推理层；
  3. **把不确定性统一进后验** —— 硬约束进支持集、歧义进候选后验，不做过早 argmax。
  核心结论：显式建模的正确粒度 = 生成过程，而非推理模块；小数据+弯曲流形 → 实例级。下一步：双层渲染残差、池外光照探针、逐 kind PPCA 似然比、大数据逃生通道。
- **证据来源**：`docs/architecture.md §7`、`§0`。

---

## 附录：核心提交速查

| 提交 | 日期 | 语义 |
|---|---|---|
| `8ed8356` | 07-24 | Initial commit |
| `aaa43e0` | 07-27 | Gabor-GMM (DGGG/DG-MRF) 两版软聚类废弃报告 + 最小生成模型提案 |
| `b52ac7a` | 07-31 | CGA implementation |
| `3a344b4` | 08-02 | add riesz.py（Riesz 替代 Gabor） |
| `cbc9fa8` | 08-04 | remove gmm.py（VB-GMM 接替） |
| `43416c8` | 08-05 | flow.md / params.md，CGA 深集成 |
| `285763a` | 08-11 | start new branch（spn-experiment） |
| `97c33d6` | 08-11 | SPN 逆渲染/场景码（N=2000 码 0.970；288 码） |
| `84d2f3e` | 08-12 | OnlineSPN v2 |
| `523b97a` | 08-13 | 主线切换：连续化、离散码退役 |
| `54cd2d1` | 08-13 | 全因子重设计 + EM 退役（实例级） |
| `fffc06a` | 08-10 | 单目深度 iBims/NYU 域失败（Spearman≈0） |
| `05ac7f0` | 08-10 | 遮挡序 0.444→0.419、T 结假阳性 76% |
