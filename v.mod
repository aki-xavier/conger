Module {
	name: 'conger'
	description: 'SPN inverse rendering research: stereo 2D images → Riesz features → MixtureSPN → full cga.Scene reconstruction (V port).'
	version: '0.1.0'
	license: 'MIT'
	// Dependencies are resolved through the project-local `.vmodules` directory
	// (symlinks set up by the Makefile): `cga` (renderer) and `mlx` (mlx-v
	// bindings). They are intentionally not listed as vpm dependencies.
	dependencies: []
}
